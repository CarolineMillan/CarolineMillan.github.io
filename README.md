# static_site_generator
A static site generator in Python that I built from the course on boot.dev

possible extensions:

- nested elements (eg. **the ability to have _italics_ inside bold text**)
- you could add more unit tests tbh